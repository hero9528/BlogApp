# Configuration for Production environment (Render)
# Spring Boot will auto-configure the datasource from the DATABASE_URL environment variable.

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update